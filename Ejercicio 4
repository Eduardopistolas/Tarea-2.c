//Problemas simples
//En una Casa de Cambio necesitan construir un programa que dado como dato una cantidad expresada en d ́olares, convierta esa cantidad en pesos.

#include <stdio.h>
int main()

{
    float dolares, pesos;

    printf("Ingrese la cantidad en dolares: ");
    scanf("%f", &dolares);

    pesos = dolares * 11.96;

    printf("Recibira en pesos: %.2f\n", pesos);

    return 0;
}
